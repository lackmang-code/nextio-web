# 홍보팀 보고 — 2026-08-23

## 완료 항목

### 1. 데일리카드 3종 쇼케이스 구축 및 라이브 배포

홈페이지 데일리카드 섹션을 "위탁제작 상품 진열장"으로 개편. 커밋 `43816bd`, `d5c0c08` 푸시 완료, 라이브 반영 확인.

**배경**: 데일리카드 섹션은 "귀사의 이름으로 발행하는 AI 에디터"라고 말하면서 정작 로고가 바뀐 카드를 한 장도 보여주지 못하고 있었다(디스플레이 데일리 1종뿐). 매거진 섹션이 3종을 진열하는 것과 대비됐다.

**만든 것**

| 브랜드 | 명의 | 성격 | 색 |
|---|---|---|---|
| 첨디공 디스플레이 데일리 | 성균관대 첨단디스플레이공학과 | 위탁제작 샘플 | 초록 `#4ADE80` |
| 반도체 데일리 | Next I/O | 자체 발행 | 파랑 `#60A5FA` |
| 배터리 데일리 | 에이프로머티리얼즈 | 위탁제작 샘플 | 주황 `#FB923C` |

각 브랜드 2일치(8/22·8/23) 카드 + 아카이브 페이지 생성. 경로는 `showcase/<브랜드키>/`.

### 2. 생성기 브랜드 파라미터화

- **`_automation/brands.json` 신설** — 제호·발행처·색·로고·URL·CTA를 브랜드 단위로 분리
- **`make_promo.py` / `make_index.py`에 `--brand` 옵션 추가**
- **회귀 검증 통과**: 옵션 없이 실행하면 패치 전과 **바이트 단위로 완전히 동일한** 카드·아카이브·사이트맵 생성. 디스플레이 데일리 실발행에 영향 없음을 확인함

### 3. 마크다운형 다이제스트 파서 신설

반도체·배터리 다이제스트는 디스플레이와 메일 형식이 완전히 다름을 발견:

- 디스플레이: `1. 제목` / `- 내용:` / `- 분석:` / `- 출처: 매체(URL)`
- 반도체·배터리: `카테고리 ## 1. [제목](URL)` 마크다운 형식, **출처 매체명 없음**

기존 파서로는 0건 파싱. `digest_md_to_items.py`를 새로 만들되 **기존 `digest_to_items.py`(실발행 전용)는 건드리지 않고** `--legacy` 옵션으로 호출만 함. 출처는 URL 도메인에서 역추정(30여 개 매체 매핑).

### 4. 기사 날짜 필터 도입 (대표 지시)

다이제스트에 오래된 기사가 섞여 있는 문제를 해결. 다이제스트 발행일보다 **3일 넘게 지난 기사는 카드에서 제외**(`--max-age`로 조정 가능).

**판정 규칙**: URL **경로**의 날짜만 인정하고 **쿼리스트링은 무시**한다. `?idxno=2023092168625` 같은 값은 날짜처럼 보여도 기사 일련번호라, 날짜로 읽으면 당일 기사가 "2023년 것"으로 잘못 걸러진다(실제 IT조선 사례). 보도일을 알 수 없는 기사는 남긴다.

**실제 제외 사례**

| 카드 | 제외된 기사 |
|---|---|
| 반도체 8/22 | 2026-03-16자 인텔 EMIB (5개월 전) |
| 반도체 8/23 | 2026-06-02자 SK하이닉스 HBF·CXL (2개월 반 전) |
| 배터리 8/23 | 2026-08-19자 필에너지 전고체 장비 |
| 성대 8/23 | 2026-08-16자 IMID 폐막 / 2026-08-19자 UDC OLEDX |

### 5. 매일 자동 발행 연결

`_automation/run_showcase.py` 신설. 브랜드별로 Gmail에서 자기 다이제스트를 찾아 파싱·카드 생성·`latest.html` 갱신까지 수행. 워크플로 `display-daily.yml`에 별도 스텝으로 추가.

- **실발행과 완전 분리**: `continue-on-error`로 감싸 쇼케이스가 실패해도 디스플레이 데일리는 정상 커밋됨. 실패는 마지막 스텝에서 job을 실패시켜 알림 발송
- **교차 오선택 검사 완료**: 세 브랜드가 각자 자기 메일 하나씩만 매칭하는 것을 확인(제목 날짜 형식이 `23일자` vs `23일 일일`로 달라 정규식을 별도로 맞춤)
- **내일(8/24) 09:45부터 3종 자동 발행 시작 예정**

### 6. 홈페이지 진열장 UI

- 브랜드 3칸 버튼 — 각 버튼이 자기 브랜드 색을 배경에 깔고, 기본 20% → hover 32% → 선택 52%로 짙어짐
- **날짜 목록 폐지** (대표 지시) — 목업이 늘 최신본을 보여주므로 중복. 과거분은 아카이브 페이지에서 확인
- 목업이 `latest.html`을 가리키도록 변경 → **발행 스크립트가 매일 이 파일을 덮어쓰므로 홈페이지는 손댈 필요 없음**
- 모바일에서도 진열장 3칸 노출(폰 목업만 숨김)

---

## 이슈 · 후속 필요

### ⚠️ 명의 사용 동의가 구두뿐 (서면 없음)

에이프로머티리얼즈·성균관대 첨단디스플레이공학과 명의 사용은 **2026-08-23 대표 구두 동의 확보**. 서면 근거 없음.

- 일회성 샘플이 아니라 **매일 자동 발행**이라 사용 강도가 높다
- 담당자 교체나 분쟁 시 근거가 대표 기억뿐 → **메일 한 통이라도 남겨둘 것을 권고**(초안 작성 가능)
- 두 브랜드 카드에는 헤더·하단 CTA 양쪽에 **"위탁제작 샘플"** 표기를 유지해 실적 오인을 방지함
- 동의 상태는 `_automation/brands.json`의 `_note` 필드에 기록해둠

### ⚠️ 로고가 임시 워드마크

에이프로·성대 로고는 실제 CI를 확보하지 못해 **임시로 제작한 텍스트 워드마크**. 실제 로고 파일 확보 시 `_automation/assets/brand_battery.svg`, `brand_skku.svg` 교체 필요.

### ⚠️ 실운영 디스플레이 데일리에도 오래된 기사 혼입

날짜 필터를 검사하면서 발견 — **현재 매일 발행 중인 디스플레이 데일리 8/23분에도 8/16·8/19자 기사가 2건 섞여 나갔다.** 샘플만의 문제가 아니다.

**실발행 파이프라인은 아직 손대지 않았다.** 적용하려면 `run_daily.py`가 쓰는 파서에 같은 필터를 넣으면 되나, 발행 중인 코드라 지시를 받고 진행할 사항.

---

## 다음 세션 착수점

1. **내일 아침 자동 발행 결과 확인** — `gh run list --workflow=display-daily.yml`로 쇼케이스 스텝 성공 여부 점검(첫 실운영이라 확인 필요)
2. 실운영 디스플레이 데일리에 날짜 필터 적용 여부 결정
3. 에이프로·성대 로고 실물 확보 시 교체
4. 명의 사용 동의 서면화(메일) 여부 결정

---

# 추가 보고 — 디플 홈페이지 점검 요청 처리 (같은 날)

디플(`04_Display_Now\`)이 올린 `_검수_홈페이지_변경요청_20260823.html` 점검 보고를 처리했다. **지적 사항을 실물로 전부 재확인한 뒤 반영**했고, 그 과정에서 **오진 1건을 발견해 정정**했다.

## 반영 완료

### 1. education 이미지 webp 전환 (커밋 `72f5a20`, `fbeccea`)

대표프로그램 소개 페이지가 png 원본을 그대로 띄우고 있어 영업 링크로 쓰기 어려운 상태였다.

| 페이지 | 이전 | 이후 | 라이브 실측 |
|---|---|---|---|
| `education/unicorn.html` | 43MB | 2.28MB | 95% ↓ |
| `education/research.html` | 23MB | 0.91MB | 96% ↓ |

- png 10장(2048~2816px) → webp(q85) 변환: **68.2MB → 3.0MB (96% 절감)**
- 해상도는 유지하고 포맷만 변경. 대표 화질 확인 후 원본 png 삭제
- **저장소 작업본 90MB → 21MB**
- ⚠️ GitHub 저장소 용량 자체는 줄지 않음(이미 히스토리에 포함). 앞으로 이미지는 커밋 전에 webp로 변환할 것

### 2. DISPLAY NOW 진열대 (커밋 `72f5a20`)

- 표지: 도안 플레이스홀더(svg) → **창간호 실제 표지**(webp 67KB, 2:3)
- 라벨: `DISPLAY NOW · 주간 발행` → `DISPLAY NOW 제1호 · 8월`
- 링크: 사이트 루트 → `/issue/1` — 주간지라 루트는 최신호로 바뀌어 1호 표지와 어긋나기 때문

### 3. 진열대 자동화 연동 (커밋 `74d5ea0`)

주간 발행이라 손으로 갱신하면 **연 50회** 작업이 된다. 디플이 `index.html`을 건드리지 않고 갱신할 수 있도록 데이터를 분리했다.

| 디플이 갱신할 파일 | 내용 |
|---|---|
| `images/magazine-cover-display-now.webp` | 표지 — **파일명 고정, 매번 덮어쓰기** |
| `data/display-now-latest.json` | 호수·라벨·링크 |

- **안전장치**: JSON을 못 읽거나 깨져도 HTML 기본값이 남아 진열대가 비지 않는다. 디플 쪽 실패가 홈페이지로 번지지 않는다
- **파일명 고정 이유**: 주간지라 호별 파일을 쌓으면 연 50장. 대신 캐시 무효화를 위해 JSON `cover`에 `?v=<호수>`를 붙인다 (**가장 흔한 실수 지점**)
- 규격서 `연동규격_DISPLAY_NOW_진열대.md` 작성 — 표지 규격·JSON 필드·변환 코드·커밋 절차·확인 명령 포함. 디플에 전달 필요

## ⚠️ 정정 — wrangler.toml은 오진이었다 (커밋 `388811f`)

디플은 "홈페이지 폴더인데 `name = "skk-display-magazine"`이라는 매거진 프로젝트 이름이 적혀 있다 → 수동 배포 시 매거진을 덮어쓴다"고 설정 오류로 보고했다.

**Cloudflare 대시보드를 직접 확인한 결과 사실이 아니었다.**

| Pages 프로젝트 | 실제 용도 | 연결 저장소 / 도메인 |
|---|---|---|
| **`skk-display-magazine`** | **회사 홈페이지** | `lackmang-code/nextio-web` → `nextio.ai.kr`, `www.nextio.ai.kr` (활성) |
| `display-magazine` | 성대 첨디공 매거진 | `display-magazine.pages.dev` |
| `display-now` | DISPLAY NOW | `display-now.nextio.ai.kr` |

이름만 매거진처럼 보일 뿐, **이 홈페이지를 배포하는 프로젝트의 실제 이름**이다. 지적대로 고쳤다면 오히려 수동 배포가 엉뚱한 프로젝트로 나갔을 것이다. Cloudflare Pages는 프로젝트명 변경이 불가하므로 정리하려면 프로젝트를 새로 만들고 도메인·D1 바인딩·GitHub 연동을 모두 옮겨야 한다 — **권장하지 않는다.**

같은 오해가 반복되지 않도록 `wrangler.toml` 상단 주석과 `CLAUDE.md` 도메인 절에 프로젝트 3종 대조표를 남겼다.

## 배포 확인 시 주의사항 (이번에 걸린 함정)

**HTTP 200만으로 배포 완료를 판단하면 안 된다.** Cloudflare Pages는 아직 모르는 경로에 404 대신 `index.html`을 반환한다. 이번에 표지·JSON 요청이 200을 반환했지만 내용은 홈페이지 HTML이었다.

→ **`content_type`으로 확인할 것** (`image/webp`, `application/json`).

## 남은 항목

| 항목 | 상태 |
|---|---|
| 첨디공 On 9월호 슬롯 | 9/1 발행 시 추가 (대표 승인 완료) |
| 시뮬레이터 노출 | **대표 판단 대기** — 비공개 페이지(noindex + 링크 미노출) 방식 제안함 |
| `magazine.nextio.ai.kr` | 대표 판단 대기 |
| 디플에 회신 전달 | 대표 경유 필요 (회신 문구 작성 완료) |
